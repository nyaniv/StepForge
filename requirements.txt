# pip dependencies (conda users: use environment.yml instead)
# Note: pythonocc-core must be installed via conda (conda-forge), not pip.
# Note: pytorch must match your CUDA version; see https://pytorch.org/get-started/locally/
#
# Version notes:
#   transformers>=4.51.3  — unsloth requires >=4.51.3; transformers 5.x works fine
#   trl==0.29.0           — defines its own _is_package_available (compatible with transformers 5.x)
#                           trl<=0.24.0 imports it from transformers and breaks with 5.x API change
#   NO llm_blender        — broken with transformers>=4.45 (imports removed TRANSFORMERS_CACHE)
#                           TRL treats it as optional and degrades cleanly
#   NO mergekit           — conflicts with accelerate>=1.7; not needed for SFT or RL training

numpy<2                    # DEP-5: pythonocc-core 7.7.2 built against numpy 1.x C-ABI
unsloth>=2024.12,<2026     # DEP-4: pin so resume sees same defaults across runs.
                           # DC-2: unsloth and trl monkey-patch the same Trainer
                           # internals — import-time-incompatible but run in
                           # separate processes (SFT script vs RL script).
transformers>=4.51.3
trl==0.29.0
peft>=0.10
accelerate                 # required by transformers trainer
sentence-transformers      # SentenceTransformer for RAG embeddings
faiss-cpu                  # FAISS retrieval (CPU is fine — only queried once, pre-training)
open3d==0.18.0             # DEP-1: 0.19+ wheels need GLIBC>=2.31
scipy                      # cKDTree for Chamfer Distance
bitsandbytes               # 4-bit/8-bit quantization
datasets
pandas
loguru
omegaconf
tqdm
